(function(){
 let C,Store,Catalog;if(typeof module!=='undefined'&&module.exports){C=require('./campaign-constants.js');Store=require('./campaign-store.js');Catalog=require('./campaign-catalog.js')}else{C=window.PBACampaign;Store=window.PBACampaign.CampaignStore;Catalog=window.PBACampaign}
 class CampaignManager{constructor(store=Store){this.store=store;this.data=store.load();this.listeners=[]} notify(e){this.listeners.forEach(f=>f(e,this.getState()))} onChange(f){if(typeof f==='function')this.listeners.push(f)} save(e){this.data=this.store.save(this.data);this.notify(e);return this.getState()} getState(){return JSON.parse(JSON.stringify(this.data))} getRosterIds(){const d=this.data;return [...d.startingRosterIds,...Catalog.MASTERS.map(m=>d.challenges[m.challengeId]?.rewardPokemonId).filter(Boolean),d.superTrainer.rewardPokemonId].filter(Boolean)} getRoster(){return this.getRosterIds().map(Catalog.byId).filter(Boolean)} getBadgeCount(){return Catalog.MASTERS.filter(m=>this.data.challenges[m.challengeId]?.badgeEarned).length} isStarted(){return this.data.startingRosterIds.length===C.START_SIZE} start(ids){if(this.isStarted())return{ok:false,reason:'IMMUTABLE'};const clean=[...new Set((ids||[]).map(Number))].filter(x=>Catalog.DRAFT.some(p=>p.id===x));if(clean.length!==C.START_SIZE)return{ok:false,reason:'REQUIRES_SIX'};this.data=this.store.getDefaultState();this.data.startingRosterIds=clean;this.data.status=C.CAMPAIGN_STATUS.ACTIVE;this.data.startedAt=new Date().toISOString();this.save('STARTED');return{ok:true}} reset(){this.data=this.store.reset();this.notify('RESET');return this.getState()} getMaster(id){return Catalog.MASTERS.find(m=>m.challengeId===id)||null} canChallenge(kind,id){if(!this.isStarted()||this.data.pendingReward)return false;if(kind==='MASTER')return !!this.getMaster(id);if(kind==='SUPER')return this.getBadgeCount()===18&&this.getRosterIds().length===24&&!this.data.superTrainer.defeated;if(kind==='SHADOW')return this.data.status===C.CAMPAIGN_STATUS.SHADOW_AVAILABLE&&!this.data.shadowTrainer.defeated;return false} getBattleConfig(kind,id,teamIds){if(!this.canChallenge(kind,id))throw new Error('Desafio indisponível.');const team=[...new Set((teamIds||[]).map(Number))];if(team.length!==3||team.some(x=>!this.getRosterIds().includes(x)))throw new Error('Escolha exatamente três Pokémon do elenco da campanha.');let enemy,meta,modifiers={};if(kind==='MASTER'){const m=this.getMaster(id);enemy=m.team.map(x=>x.id);meta={mode:'CAMPAIGN',kind,id,opponentName:`${m.trainerName} — ${m.trainerTitle}`}}else if(kind==='SUPER'){enemy=Catalog.SUPER_TEAM.map(x=>x.id);meta={mode:'CAMPAIGN',kind,opponentName:'Super Treinador'}}else{enemy=Catalog.SUPER_TEAM.map(x=>x.id);modifiers={SHADOW_AURA:true};meta={mode:'CAMPAIGN',kind,opponentName:'Desafio Final',modifiers}}return{playerTeamIds:team,enemyTeamIds:enemy,metadata:meta,modifiers}} recordBattle(result){const {battleId,kind,id,winner}=result||{};if(typeof battleId!=='string'||this.data.processedBattleIds.includes(battleId))return{processed:false,duplicate:true};this.data.processedBattleIds.push(battleId);this.data.processedBattleIds=this.data.processedBattleIds.slice(-C.MAX_PROCESSED);if(kind==='MASTER'){const r=this.data.challenges[id]||{attempts:0,badgeEarned:false,rewardPokemonId:null,rewardBattleId:null};r.attempts++;this.data.challenges[id]=r;if(winner==='player'&&!r.badgeEarned){r.badgeEarned=true;r.rewardBattleId=battleId;this.data.pendingReward={kind:'MASTER',challengeId:id,candidates:this.getMaster(id).team.map(x=>x.id)}}}else if(kind==='SUPER'){this.data.superTrainer.attempts++;if(winner==='player'&&!this.data.superTrainer.defeated){this.data.superTrainer.defeated=true;this.data.pendingReward={kind:'SUPER',challengeId:null,candidates:Catalog.SUPER_TEAM.map(x=>x.id)};this.data.status=C.CAMPAIGN_STATUS.SUPER_REWARD_PENDING}}else if(kind==='SHADOW'){this.data.shadowTrainer.attempts++;if(winner==='player'){this.data.shadowTrainer.defeated=true;this.data.shadowTrainer.battleId=battleId;this.data.status=C.CAMPAIGN_STATUS.COMPLETED}}this.save('BATTLE_RECORDED');return{processed:true,pendingReward:this.data.pendingReward}} claimReward(pokemonId){const p=this.data.pendingReward,id=Number(pokemonId);if(!p||!p.candidates.includes(id))return{ok:false,reason:'INVALID_REWARD'};if(p.kind==='MASTER'){const r=this.data.challenges[p.challengeId];r.rewardPokemonId=id;if(this.getBadgeCount()===18)this.data.status=C.CAMPAIGN_STATUS.SUPER_AVAILABLE}else{this.data.superTrainer.rewardPokemonId=id;this.data.status=C.CAMPAIGN_STATUS.SHADOW_AVAILABLE;this.data.shadowTrainer.revealed=true}this.data.pendingReward=null;this.save('REWARD_CLAIMED');return{ok:true}} }
 const api={CampaignManager};if(typeof module!=='undefined'&&module.exports)module.exports=api;else{window.PBACampaign=window.PBACampaign||{};Object.assign(window.PBACampaign,api)}
})();
